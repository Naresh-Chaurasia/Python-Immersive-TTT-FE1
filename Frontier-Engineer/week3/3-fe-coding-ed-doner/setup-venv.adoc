[source,asciidoc, role="wrap"]
----
rm -rf .venv

/opt/homebrew/bin/python3.13 -m venv .venv        # or use uv: uv venv --python 3.12 .venv

source .venv/bin/activate


which python
which pip


pip install ipykernel
pip install -r requirements-ed-doner.txt

python -m ipykernel install --user \
   --name=.venv_ed_donar \
   --display-name "Python (.venv_ed_donar)"
----